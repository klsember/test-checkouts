version: 2.1

parameters: 
  global-param:
    type: string

jobs:
  update-packages:
    docker:
      - image: cimg/node:lts
    steps:
      - checkout
      - run: echo "testing this"
      - run: echo <<parameters.pipeline.global-param>>
workflows:
  main:
    jobs:
      - update-packages